import BrandClient from "./BrandClient";

export const metadata = {
    title: "Brand Management | Matrix Core",
    description: "Orchestrate product brand identities",
};

export default function BrandsPage() {
    return <BrandClient />;
}
