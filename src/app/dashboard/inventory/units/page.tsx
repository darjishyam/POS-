import UnitClient from "./UnitClient";

export const metadata = {
    title: "Unit Management | Matrix Core",
    description: "Define product measurement standards",
};

export default function UnitsPage() {
    return <UnitClient />;
}
